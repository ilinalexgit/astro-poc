---
import Container from "@components/Container.astro";
import Link from "@components/ui/Link.astro";
import { Image } from "astro:assets";
import {
    ArrowPathIcon,
    Bars3Icon,
    ChartPieIcon,
    CursorArrowRaysIcon,
    FingerPrintIcon,
    SquaresPlusIcon,
    XMarkIcon,
} from "@heroicons/react/24/outline";
import YmLogo from "@assets/ym-logo.png";
import { languageTag } from '../paraglide/runtime';

const pathName = Astro.url.pathname.replace(`/${languageTag()}`, '');

const menuitems = [
    {
        title: "Features",
        path: "#",
        children: [
            { title: "Action", path: "/" },
            { title: "Another action", path: "#" },
            { title: "Dropdown Submenu", path: "#" },
            { title: "404 Page", path: "/404" },
        ],
    },
    {
        title: "Pricing",
        path: "/pricing",
    },
    {
        title: "About",
        path: "/about",
    },
    {
        title: "Blog",
        path: "/blog",
    },
    {
        title: "Contact",
        path: "/contact",
    },
];
---

<Container>
    <header class="flex flex-col md:flex-row justify-between items-center relative z-10">
        <nav
            aria-label="Global"
            class="mx-auto flex max-w-7xl items-center justify-between p-6 lg:px-8 w-full">
            <div class="flex lg:flex-1">
                <a href="#" class="h-16">
                    <span class="sr-only">Y Meadows</span>
                    <Image
                        class="min-h-16 min-w-64"
                        src={YmLogo}
                        alt="Y Meadows Logo."
                        height="64"
                    />
                </a>
            </div>
            <div class="flex">
                <div class="flex lg:hidden">
                    <button
                        type="button"
                        class="-m-2.5 inline-flex items-center justify-center rounded-md p-2.5 text-gray-700">
                        <span class="sr-only">Open main menu</span>
                        <Bars3Icon aria-hidden="true" className="size-6" />
                    </button>
                </div>
                <div class="hidden lg:flex lg:gap-x-12">
                    <a href="#" class="text-sm/6 font-semibold text-gray-900">
                        Solutions
                    </a>
                    <a href="#" class="text-sm/6 font-semibold text-gray-900">
                        Resources
                    </a>
                    <a href="#" class="text-sm/6 font-semibold text-gray-900">
                        Blog
                    </a>
                    <a href="#" class="text-sm/6 font-semibold text-gray-900">
                        About
                    </a>
                    <ul>
                        <li>
                            <a href={`/fr/${pathName}`}>French</a>
                        </li>
                        <li>
                            <a href={`/en/${pathName}`}>English</a>
                        </li>
                    </ul>
                </div>
            </div>
        </nav>
        <!-- <div class="flex w-full md:w-auto items-center justify-between">
            <a href="/" class="text-lg"
                ><span class="font-bold text-slate-800">Astro</span><span
                    class="text-slate-500">ship</span
                >
            </a>
            <div class="block md:hidden">
                <MenuIcon class="w-4 h-4 text-gray-800" />
            </div>
        </div> -->
        <!-- <MenuItems class="hidden w-full md:w-auto mt-2 md:flex md:mt-0">
        <ul class="flex flex-col md:flex-row md:gap-3">
          {
            menuitems.map((item, index) => (
              <>
                {item.children && (
                  <Dropdown
                    title={item.title}
                    children={item.children}
                    lastItem={index === menuitems.length - 1}
                  />
                )}

                {!item.children && (
                  <li>
                    <a
                      href={item.path}
                      class="flex md:px-3 py-2 items-center text-gray-600 hover:text-gray-900">
                      <span> {item.title}</span>
                      {item.badge && (
                        <span class="ml-1 px-2 py-0.5 text-[10px] animate-pulse font-semibold uppercase text-white bg-indigo-600 rounded-full">
                          New
                        </span>
                      )}
                    </a>
                  </li>
                )}
              </>
            ))
          }
        </ul>
        <div class="md:hidden flex items-center mt-3 gap-4">
          <Link href="#" style="muted" block size="md">Log in</Link>
          <Link href="#" size="md" block>Sign up</Link>
        </div>
      </MenuItems> -->
    </header>
</Container>
